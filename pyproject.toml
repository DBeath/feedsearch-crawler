[tool.pytest.ini_options]
minversion = "6.0"
testpaths = [
    "tests"
]
norecursedirs = [
    "env",
    "tmp*",
    "build",
    "dist",
    "experiments"
]
filterwarnings = [
    "error",
    "ignore::UserWarning",
    "ignore::DeprecationWarning"
]